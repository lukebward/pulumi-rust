[package]
name = "pulumi_simple"
version = "2.0.0"
edition = "2021"

# Deliberately a member of the provider's workspace (see ../../Cargo.toml):
# `pulumi` is inherited so this crate and the provider binary share one copy of
# the core SDK.
[dependencies]
pulumi = { workspace = true }
