[package]
name = "aws-rs-webserver"
version = "0.1.0"
edition = "2021"

[dependencies]
pulumi = { path = "../../sdk/rust/pulumi" }
# Generate this SDK first:
#   pulumi package gen-sdk aws --language rust --out ./sdks
pulumi_aws = { path = "./sdks/aws" }

[workspace]
