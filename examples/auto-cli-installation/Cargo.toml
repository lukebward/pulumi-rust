[package]
name = "auto-cli-installation"
version = "0.1.0"
edition = "2021"

[dependencies]
pulumi = { path = "../../sdk/rust/pulumi" }
semver = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
