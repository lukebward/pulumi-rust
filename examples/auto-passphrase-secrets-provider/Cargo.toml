[package]
name = "auto-passphrase-secrets-provider"
version = "0.1.0"
edition = "2021"

[dependencies]
pulumi = { path = "../../sdk/rust/pulumi" }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync"] }
